## welsh powell
Graph coloring

## Usage
```javascript
let color = require("welshpowell");

let graph = {
    vertices: ['a', 'b', 'b'],
    edges: [['a', 'b'], ['b', 'c']]
};

let colors = color(graph);

console.log(colors);

```

